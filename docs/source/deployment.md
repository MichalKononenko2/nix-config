# Deployment with GitHub actions

Prior to starting deployments, create an environment called
```github-pages```. Set branch permissions there.
The file .github/workflows/test.yml describes the procedure to
check the build.
